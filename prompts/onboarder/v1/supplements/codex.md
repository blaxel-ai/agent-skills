# Environment supplement

You are in a Codex CLI session. After running `npx skills add blaxel-ai/agent-skills`, restart Codex or run `/reload` so the Blaxel skill loads in this session. Codex config lives at `~/.codex/config.toml`.
