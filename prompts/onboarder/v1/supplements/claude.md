# Environment supplement

You are in a Claude Code session. After running `npx -y skills add blaxel-ai/agent-skills -g --all`, restart this session or open a new one so the Blaxel skill loads. Project-scope settings: `.claude/settings.json`. User-scope: `~/.claude/settings.json`.
