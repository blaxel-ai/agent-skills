# Environment supplement

You are in Cursor. After running `npx -y skills add blaxel-ai/agent-skills -g --all`, reload the Cursor window so the Blaxel skill loads. MCP config: `~/.cursor/mcp.json`. Project rules: `.cursorrules`.
